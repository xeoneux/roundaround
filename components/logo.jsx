import React from "react";
import styled from "@emotion/styled";

const LogoWrapper = styled.div`
  padding-left: 1em;
`;

export default () => (
  <LogoWrapper>
    <h1>roundAround</h1>
  </LogoWrapper>
);
